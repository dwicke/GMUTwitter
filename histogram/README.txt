To build the project you need to have apache maven 
install on your machine. Download the executable
and unzip. Then edit the PATH enviroment variable
to include bin/mvn. Or you can download eclipse for
java developers and install the m2eclipse plugin.
Select File->import and under maven
select existing maven project. 
